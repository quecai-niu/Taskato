# v2.06 三项优化 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 v2.06 三项增强中的 UI/逻辑缺陷：×按钮不居中、每日总结跨创建日期遗漏、历史筛选UI风格不一致

**Architecture:** 3 个独立任务，互不依赖，可并行执行。Task 1 单行 XAML；Task 2 DatabaseService 新增 2 个查询方法 + DailySummaryViewModel 双查询合并；Task 3 HistoryWindow XAML 筛选区重绘（RadioButton→Chip 样式 + ComboBox 深色模板 + MVVM 绑定）

**Tech Stack:** WPF .NET 8, SQLite (sqlite-net-pcl), MVVM

---

### Task 1: 番茄钟 × 删除按钮垂直居中

**Files:**
- Modify: `src/Taskato/Views/MainWindow.xaml:515`

- [ ] **Step 1: 修改 Padding**

`MainWindow.xaml` 第 515 行，将 × 按钮的 Padding 从 `"2,0"` 改为 `"4,2"`：

```xml
<!-- 改前 -->
Margin="6,0,0,0" Padding="2,0"

<!-- 改后 -->
Margin="6,0,0,0" Padding="4,2"
```

- [ ] **Step 2: 编译 + 冒烟**

```bash
dotnet build src/Taskato/Taskato.csproj
```

确认编译通过，无 XAML 标记错误。

- [ ] **Step 3: Commit**

```bash
git add src/Taskato/Views/MainWindow.xaml
git commit -m "fix: 附加番茄钟×删除按钮垂直居中 — Padding 2,0→4,2"
```

---

### Task 2: 每日总结改为混合口径（已完成按 CompletedAt，未完成按 CreatedAt）

**Files:**
- Modify: `src/Taskato/Services/DatabaseService.cs`
- Modify: `src/Taskato/ViewModels/DailySummaryViewModel.cs`

- [ ] **Step 1: DatabaseService 新增 GetTasksCompletedOnAsync**

在 `DatabaseService.cs` 的 `SearchTasksAsync` 方法之后（约第 153 行），添加：

```csharp
/// <summary>
/// 查询指定日期内完成的所有任务（不限创建日期）
/// </summary>
public async Task<List<TaskItem>> GetTasksCompletedOnAsync(DateTime date)
{
    var dayStart = date.Date;
    var dayEnd = dayStart.AddDays(1);
    return await _db.Table<TaskItem>()
        .Where(t => t.CompletedAt >= dayStart && t.CompletedAt < dayEnd)
        .ToListAsync();
}

/// <summary>
/// 查询指定日期及之前创建的未完成任务
/// </summary>
public async Task<List<TaskItem>> GetUncompletedCreatedUpToAsync(DateTime date)
{
    var dayEnd = date.Date.AddDays(1);
    return await _db.Table<TaskItem>()
        .Where(t => t.CreatedAt < dayEnd && !t.IsCompleted)
        .ToListAsync();
}
```

- [ ] **Step 2: 编译确认新方法无语法错误**

```bash
dotnet build src/Taskato/Taskato.csproj
```

- [ ] **Step 3: DailySummaryViewModel.LoadSummaryAsync 改为双查询合并**

修改 `DailySummaryViewModel.cs` 的 `LoadSummaryAsync` 方法（第 137-164 行），替换为：

```csharp
/// <summary>
/// 加载当前日期的任务数据并刷新统计
/// 已完成按 CompletedAt 查询（不限创建日期），未完成按 CreatedAt ≤ 今天查询
/// </summary>
public async Task LoadSummaryAsync()
{
    var completed = await _dbService.GetTasksCompletedOnAsync(CurrentDate);
    var uncompleted = await _dbService.GetUncompletedCreatedUpToAsync(CurrentDate);

    TotalCount = completed.Count + uncompleted.Count;
    CompletedCount = completed.Count;
    CompletionRate = TotalCount > 0 ? $"{(double)CompletedCount / TotalCount:P0}" : "N/A";
    HighPriorityCompleted = completed.Count(t => t.Priority >= 2);

    if (TotalCount == 0)
        SummaryLine = "今天没有任务记录。";
    else if (uncompleted.Count == 0)
        SummaryLine = $"今天完成了全部 {TotalCount} 个任务，太棒了！";
    else
        SummaryLine = $"今天完成了 {CompletedCount}/{TotalCount} 个任务，再接再厉！";

    CompletedTasks.Clear();
    foreach (var t in completed.OrderByDescending(t => t.Priority))
        CompletedTasks.Add(t);

    UncompletedTasks.Clear();
    foreach (var t in uncompleted.OrderByDescending(t => t.Priority))
        UncompletedTasks.Add(t);
}
```

- [ ] **Step 4: 编译确认**

```bash
dotnet build src/Taskato/Taskato.csproj
```

- [ ] **Step 5: Commit**

```bash
git add src/Taskato/Services/DatabaseService.cs src/Taskato/ViewModels/DailySummaryViewModel.cs
git commit -m "fix: 每日总结改为混合口径 — 已完成按CompletedAt筛选，未完成按CreatedAt筛选"
```

---

### Task 3: 历史查询筛选UI统一风格 + ComboBox 深色模板 + MVVM 绑定

**Files:**
- Modify: `src/Taskato/Views/HistoryWindow.xaml`
- Modify: `src/Taskato/Views/HistoryWindow.xaml.cs`

- [ ] **Step 1: HistoryWindow.xaml 筛选区 RadioButton → Chip 样式**

替换 `HistoryWindow.xaml` 第 242-258 行（状态筛选行）：

```xml
<!-- 状态筛选行 — Chip 按钮组 -->
<StackPanel Orientation="Horizontal" Margin="0,0,0,8">
    <TextBlock Text="筛选" FontSize="13"
               Foreground="{StaticResource TextSecondaryBrush}"
               VerticalAlignment="Center" Margin="0,0,12,0"/>
    <RadioButton Content="全部" GroupName="StatusGroup"
                 VerticalAlignment="Center" Margin="0,0,6,0"
                 IsChecked="{Binding StatusFilter, Converter={StaticResource IntToBoolConverter}, ConverterParameter=0}">
        <RadioButton.Style>
            <Style TargetType="RadioButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="chip" CornerRadius="8" Padding="10,5"
                                    Background="#1A6366F1" BorderBrush="Transparent" BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="chip" Property="Background" Value="#336366F1"/>
                                    <Setter TargetName="chip" Property="BorderBrush" Value="#6366F1"/>
                                    <Setter TargetName="chip" Property="BorderThickness" Value="1"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="chip" Property="Background" Value="#256366F1"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </RadioButton.Style>
    </RadioButton>
    <RadioButton Content="未完成" GroupName="StatusGroup"
                 VerticalAlignment="Center" Margin="0,0,6,0"
                 IsChecked="{Binding StatusFilter, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}">
        <RadioButton.Style>
            <Style TargetType="RadioButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="chip" CornerRadius="8" Padding="10,5"
                                    Background="#1A6366F1" BorderBrush="Transparent" BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="chip" Property="Background" Value="#336366F1"/>
                                    <Setter TargetName="chip" Property="BorderBrush" Value="#6366F1"/>
                                    <Setter TargetName="chip" Property="BorderThickness" Value="1"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="chip" Property="Background" Value="#256366F1"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </RadioButton.Style>
    </RadioButton>
    <RadioButton Content="已完成" GroupName="StatusGroup"
                 VerticalAlignment="Center"
                 IsChecked="{Binding StatusFilter, Converter={StaticResource IntToBoolConverter}, ConverterParameter=2}">
        <RadioButton.Style>
            <Style TargetType="RadioButton" BasedOn="{StaticResource {x:Type ToggleButton}}">
                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="chip" CornerRadius="8" Padding="10,5"
                                    Background="#1A6366F1" BorderBrush="Transparent" BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="chip" Property="Background" Value="#336366F1"/>
                                    <Setter TargetName="chip" Property="BorderBrush" Value="#6366F1"/>
                                    <Setter TargetName="chip" Property="BorderThickness" Value="1"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="chip" Property="Background" Value="#256366F1"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </RadioButton.Style>
    </RadioButton>
</StackPanel>
```

- [ ] **Step 2: HistoryWindow.xaml ComboBox 替换为深色模板 + SelectedValue 绑定**

替换第 273-281 行：

```xml
<ComboBox Grid.Column="1" x:Name="SortFieldCombo"
          FontSize="12"
          Foreground="{StaticResource TextPrimaryBrush}"
          SelectedValuePath="Tag"
          SelectedValue="{Binding SortField, Mode=TwoWay}">
    <ComboBox.Resources>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="#252540"/>
        </Style>
    </ComboBox.Resources>
    <ComboBox.Template>
        <ControlTemplate TargetType="ComboBox">
            <Grid>
                <ToggleButton x:Name="ToggleButton"
                              Background="#1A6366F1" BorderBrush="#666366F1" BorderThickness="1"
                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                              ClickMode="Press" Padding="12,7" Cursor="Hand">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="bd" CornerRadius="8"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" VerticalAlignment="Center"
                                                      Content="{Binding RelativeSource={RelativeSource AncestorType=ComboBox}, Path=Text}"/>
                                    <Path Grid.Column="1" Data="M0,0 L4,4 L8,0" Stroke="#9CA3AF" StrokeThickness="1.5"
                                          Fill="Transparent" Width="8" Height="5" Margin="8,0,0,0"
                                          VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="#256366F1"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
                <Popup x:Name="Popup"
                       Placement="Bottom"
                       IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                       AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                    <Border x:Name="DropDownBorder"
                            Background="#252540" BorderBrush="#666366F1" BorderThickness="1"
                            CornerRadius="8" Margin="0,2,0,0" MaxHeight="200">
                        <ScrollViewer>
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>
        </ControlTemplate>
    </ComboBox.Template>
    <ComboBoxItem Content="发布时间" Tag="CreatedAt"/>
    <ComboBoxItem Content="完成时间" Tag="CompletedAt"/>
    <ComboBoxItem Content="最后修改时间" Tag="LastModifiedAt"/>
</ComboBox>
```

- [ ] **Step 3: 日期 DropShadowEffect Opacity 微调**

第 1 个日期 Border（约第 143 行）和第 2 个日期 Border（约第 159 行），`DropShadowEffect` 的 `Opacity` 从 `"0.2"` 改为 `"0.35"`：

```xml
<!-- 改前 -->
<DropShadowEffect Color="#A855F7" BlurRadius="8" ShadowDepth="0" Opacity="0.2"/>
<!-- 改后 -->
<DropShadowEffect Color="#A855F7" BlurRadius="8" ShadowDepth="0" Opacity="0.35"/>
```

两处都改。

- [ ] **Step 4: HistoryWindow.xaml.cs 删除 RadioButton 和 ComboBox 事件处理**

在 `HistoryWindow.xaml.cs` 中删除 `StatusFilter_Click`（第 99-107 行）、`SortFieldCombo_SelectionChanged`（第 112-121 行）。`SortDirectionBtn_Click` 和 `PriorityFirst_Changed` 保留不动。

同时从 XAML 中移除 `StatusFilter_Click` 和 `SortFieldCombo_SelectionChanged` 的事件绑定属性。

> 注：`IntToBoolConverter` 已在 `App.xaml:23` 全局注册，`HistoryViewModel.StatusFilter` 属性已存在（第 56-60 行），无需新建。

- [ ] **Step 5: 编译 + 冒烟**

```bash
dotnet build src/Taskato/Taskato.csproj
```

确认编译通过并验证 ComboBox 模板中的 `{Binding RelativeSource={RelativeSource AncestorType=ComboBox}, Path=Text}` 绑定在 WPF 中正常工作。

- [ ] **Step 6: Commit**

```bash
git add src/Taskato/Views/HistoryWindow.xaml src/Taskato/Views/HistoryWindow.xaml.cs
git commit -m "feat: 历史查询筛选UI统一风格 — Chip按钮组+ComboBox深色模板+MVVM绑定"
```

---

### 验证清单

全部 Task 完成后：

- [ ] `dotnet build src/Taskato/Taskato.csproj` 零错误
- [ ] 启动应用，切到番茄钟 → 附加组 × 按钮视觉居中
- [ ] 每日总结：创建一个旧日期的任务并完成 → 切换到当天 → 任务出现在已完成列表
- [ ] 历史查询：筛选 Chip 选中态高亮紫色边框、ComboBox 下拉深色背景浅色字可读
